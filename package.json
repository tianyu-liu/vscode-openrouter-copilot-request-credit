{
  "name": "openrouter-copilot-request-credit",
  "displayName": "OpenRouter for Copilot with Custom Request & Credit Check",
  "description": "OpenRouter provider for Copilot Chat with one control panel: paste a request body from the OpenRouter Request Builder and every chat request follows it, while the status bar tracks your key's credit usage.",
  "version": "0.1.0",
  "publisher": "tianyu-liu",
  "repository": {
    "type": "git",
    "url": "https://github.com/tianyu-liu/vscode-openrouter-copilot-request-credit.git"
  },
  "engines": {
    "vscode": "^1.121.0"
  },
  "enabledApiProposals": [
    "chatProvider",
    "languageModelThinkingPart"
  ],
  "categories": [
    "AI",
    "Chat"
  ],
  "keywords": [
    "openrouter",
    "copilot",
    "byok",
    "quantization",
    "credit"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "languageModelChatProviders": [
      {
        "vendor": "openrouter-copilot-request-credit",
        "displayName": "OpenRouter: RC",
        "managementCommand": "openrouterCopilot.manage"
      }
    ],
    "commands": [
      {
        "command": "openrouterCopilot.manage",
        "title": "OpenRouter: Manage provider"
      },
      {
        "command": "openrouterCopilot.pasteTemplate",
        "title": "OpenRouter: Paste custom request"
      },
      {
        "command": "openrouterCopilot.clearTemplate",
        "title": "OpenRouter: Clear custom request"
      }
    ],
    "configuration": [
      {
        "title": "OpenRouter for Copilot",
        "properties": {
          "openrouterCopilot.baseUrl": {
            "type": "string",
            "default": "https://openrouter.ai/api/v1",
            "description": "OpenRouter API base URL used by the Copilot Chat provider."
          },
          "openrouterCopilot.creditLimit": {
            "type": "number",
            "default": 0,
            "minimum": 0,
            "scope": "application",
            "description": "Your local cap in USD — shown as your limit when the key has no cap of its own (`0` disables the guardrail and shows the account balance). Application-scoped: workspace overrides are ignored."
          },
          "openrouterCopilot.creditResetPeriod": {
            "type": "string",
            "enum": [
              "daily",
              "weekly",
              "monthly",
              "never"
            ],
            "default": "daily",
            "scope": "application",
            "description": "Reset cadence for the local limit when the key has no credit cap of its own (daily/weekly/monthly/never). Application-scoped: workspace overrides are ignored."
          },
          "openrouterCopilot.creditIncludeByok": {
            "type": "boolean",
            "default": true,
            "scope": "application",
            "description": "Count bring-your-own-key (BYOK) usage toward the guardrail remaining balance. Application-scoped: workspace overrides are ignored."
          },
          "openrouterCopilot.creditRefreshIntervalMinutes": {
            "type": "number",
            "default": 5,
            "minimum": 1,
            "maximum": 1440,
            "scope": "application",
            "description": "How often (in minutes) to refresh usage from OpenRouter (clamped to 1-1440). Application-scoped: workspace overrides are ignored."
          },
          "openrouterCopilot.creditBaseUrl": {
            "type": "string",
            "default": "https://openrouter.ai",
            "scope": "application",
            "description": "OpenRouter base URL for the credit/usage check (advanced). https-only; workspace overrides are ignored so a project cannot redirect your key to another host."
          }
        }
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "vsce package --no-dependencies",
    "test": "npm run compile && node out/test/runTest.js"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.6",
    "@types/node": "^22.0.0",
    "@types/vscode": "^1.104.0",
    "@vscode/test-electron": "^2.4.1",
    "@vscode/vsce": "^3.2.0",
    "mocha": "^10.4.0",
    "typescript": "^5.5.0"
  }
}
